# yaml-language-server: $schema=https://raw.githubusercontent.com/Azure/azure-dev/main/schemas/v1.0/azure.yaml.json
name: doc-intelligence-demo
metadata:
  template: doc-intelligence-demo
services:
  backend:
    project: .
    language: py
    host: containerapp
    docker:
      path: ./Dockerfile
